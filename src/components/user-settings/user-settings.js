import React from 'react';

class UserSettings extends React.Component {
  render() {
    return (
      <div>
        <p>this is where we render the normal user settings page</p>
        <p>take user data from store and render settings to change here</p>
      </div>
    );
  }
}

export default UserSettings;
