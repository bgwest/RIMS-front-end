import React from 'react';
import Accounts from '../manage-accounts/manage-accounts';

class AdminSettings extends React.Component {
  render() {
    return (
      <section>
        <p>This is where we render the admin instance of the settings menu.</p>
        <p>an example compontent options that would give option to render here:</p>
        <p>site-branding, manage-accounts</p>
      </section>
    );
  }
}

export default AdminSettings;
